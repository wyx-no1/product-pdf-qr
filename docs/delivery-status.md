# 交付状态

更新时间：2026-07-31

## 当前阶段

项目仍处于**治理与质量基线阶段**。业务需求第一版已存在，治理规则、决策登记表和技术栈无关的质量门禁已建立；业务负责人已确认第二轮 7 条补充决策，但需求确认门禁尚未通过，也未自动触发架构评审。技术栈尚未选择，仓库没有功能代码、依赖清单、数据库或部署配置。

## 已完成事项

- 确认 `docs/requirements-v1.md` 为业务事实来源，并保持其内容不变。
- 建立仓库协作治理规则，明确 Issue、独立 Worktree、独立分支、PR、人工合并和生产资产隔离要求。
- 登记用户可见业务待决项，并为技术不确定项采用安全、可测试、可回滚的默认值。
- 写入 2026-07-31 业务负责人确认的 7 条补充决策：Excel 混合失败语义、编码格式、编码大写保存、二维码文件命名、重复 PDF 拒绝、密码重置机制和历史 PDF 永久保留。
- 为被业务结论覆盖的 T-03、T-04、T-05 保留覆盖记录和迁移影响，未无痕改写决策历史。
- 建立从需求确认到回滚演练的质量门禁，区分强制阻塞与告警不阻塞，并定义可判定证据。
- 建立通用忽略规则，覆盖本地机密、依赖、构建产物、缓存、上传、编辑器和日志文件。
- 新增 `agent-orchestrator.yaml` 项目约定文件，声明测试失败、审核要求修改、冲突和可合并状态均只提醒，不自动返工、不自动合并；其是否被工具自动读取尚待验证。

## 待处理事项

- 由业务负责人确认 B-01、B-04、B-07 以及 B-06 的执行责任人子项。
- 完成需求确认门禁，形成业务负责人批准记录和可测试验收条件。
- 在所有业务阻塞项关闭并由人工确认 G-01 通过后，才可另行启动架构评审；本轮不自动触发。
- 技术栈确定后，为构建、类型检查、lint/format、测试、安全检查和容器构建补充精确命令与批准阈值。
- 为后续实现任务分别创建 GitHub Issue，并安排独立测试设计者。

## 阻塞事项

以下条目仍未关闭，不阻塞本次治理文档交付，但会阻塞所注明的后续阶段：

- `B-04 误导入垃圾编码的处置`：阻塞产品状态模型定稿。
- `B-01 扫码后 PDF 的呈现方式`：阻塞公网扫码体验验收。
- `B-06 管理员密码重置机制与责任`的执行责任人子项：机制已确认，但责任人及其服务器访问能力未指定，阻塞上线运维准备与公网发布。
- `B-07 二维码地址泄露后的处置立场`：阻塞公网风险接受与人工发布批准。

## 下一个门禁

1. **G-01 需求确认（强制阻塞，尚未通过）**：由业务负责人关闭 B-01、B-04、B-07 和 B-06 执行责任人子项，并形成完整的人工批准记录。
2. **G-02 架构评审（未触发）**：只有 G-01 经人工确认通过后才能另行启动；本轮不得自动进入该阶段。

在这两个门禁通过前，不应开始功能实现、创建数据库或部署服务。
