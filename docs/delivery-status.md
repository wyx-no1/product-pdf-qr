# 交付状态

更新时间：2026-07-31

## 当前阶段

项目仍处于**治理与质量基线阶段**。`docs/requirements-v2.md` 已完整收编 v1、第二轮 7 条决策和第三轮 4 条决策，成为当前唯一有效的业务事实来源；`docs/requirements-v1.md` 仅保留为历史版本。决策登记表中的业务待确认项已全部关闭，但 G-01 需求确认门禁仍缺少完整人工批准记录，因此尚未通过，也未触发 G-02 架构评审。技术栈尚未选择，仓库没有功能代码、依赖清单、数据库或部署配置。

## 已完成事项

- 保持 `docs/requirements-v1.md` 字节级不变，并创建 `docs/requirements-v2.md` 作为当前唯一有效业务事实来源；v1 仅供历史追溯。
- 建立仓库协作治理规则，明确 Issue、独立 Worktree、独立分支、PR、人工合并和生产资产隔离要求。
- 登记用户可见业务待决项，并为技术不确定项采用安全、可测试、可回滚的默认值。
- 写入 2026-07-31 业务负责人确认的 7 条补充决策：Excel 混合失败语义、编码格式、编码大写保存、二维码文件命名、重复 PDF 拒绝、密码重置机制和历史 PDF 永久保留。
- 写入 2026-07-31 业务负责人确认的最后 4 条规则：扫码呈现允许设备差异、错误编码停用后另建、公开链接可转发且停用即失效、业务负责人本人执行密码重置。
- 决策登记表的 B-01 至 B-09 均已确认，不再存在业务待确认或部分确认状态。
- 为被业务结论覆盖的 B-01、T-03、T-04、T-05 保留覆盖记录和迁移影响，未无痕改写决策历史。
- 建立从需求确认到回滚演练的质量门禁，区分强制阻塞与告警不阻塞，并定义可判定证据。
- 建立通用忽略规则，覆盖本地机密、依赖、构建产物、缓存、上传、编辑器和日志文件。
- 新增 `agent-orchestrator.yaml` 项目约定文件，声明测试失败、审核要求修改、冲突和可合并状态均只提醒，不自动返工、不自动合并；其是否被工具自动读取尚待验证。

## 待处理事项

- 为 G-01 需求确认门禁补齐完整的人工批准记录，并由门禁责任方明确判定通过；业务规则已确认不等于门禁自动通过。
- 只有 G-01 经人工判定通过后，才可由新任务另行启动 G-02 架构评审；本轮不自动触发。
- 技术栈确定后，为构建、类型检查、lint/format、测试、安全检查和容器构建补充精确命令与批准阈值。
- 为后续实现任务分别创建 GitHub Issue，并安排独立测试设计者。
- 如需让工具消费 `agent-orchestrator.yaml`，必须先单独验证权威 schema 与实际读取行为；当前文件只表达流程意图。

## 阻塞事项

- **业务阻塞项：0。** B-01 至 B-09 均已由业务负责人确认。
- **流程阻塞项：G-01 尚未通过。** 缺少门禁要求的完整人工批准记录和明确通过结论，因此不得进入架构评审或实现阶段。

## 下一个门禁

1. **G-01 需求确认（强制阻塞，尚未通过）**：核对 `docs/requirements-v2.md` 的完整性与可测试性，形成完整人工批准记录，并由责任方明确判定通过。
2. **G-02 架构评审（未触发）**：只有 G-01 经人工判定通过后才能由后续任务另行启动；本轮不得自动进入该阶段。

在这两个门禁通过前，不应开始功能实现、创建数据库或部署服务。
