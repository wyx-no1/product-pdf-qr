# 交付状态

更新时间：2026-07-31

## 当前阶段

项目处于**治理与质量基线阶段**。业务需求第一版已存在，治理规则、决策登记表和技术栈无关的质量门禁已建立。技术栈尚未选择，仓库没有功能代码、依赖清单、数据库或部署配置。

## 已完成事项

- 确认 `docs/requirements-v1.md` 为业务事实来源，并保持其内容不变。
- 建立仓库协作治理规则，明确 Issue、独立 Worktree、独立分支、PR、人工合并和生产资产隔离要求。
- 登记用户可见业务待决项，并为技术不确定项采用安全、可测试、可回滚的默认值。
- 建立从需求确认到回滚演练的质量门禁，区分强制阻塞与告警不阻塞，并定义可判定证据。
- 建立通用忽略规则，覆盖本地机密、依赖、构建产物、缓存、上传、编辑器和日志文件。

## 待处理事项

- 由业务负责人逐项确认 `docs/decision-register-v1.md` 的业务决策。
- 完成需求确认门禁，形成业务负责人批准记录和可测试验收条件。
- 在业务阻塞项关闭后开展架构评审；技术栈只能在该评审中选择。
- 技术栈确定后，为构建、类型检查、lint/format、测试、安全检查和容器构建补充精确命令与批准阈值。
- 为后续实现任务分别创建 GitHub Issue，并安排独立测试设计者。

## 阻塞事项

以下条目不阻塞本次治理文档交付，但会阻塞所注明的后续阶段：

- `B-02 产品编码格式`：阻塞产品创建、格式错误判定和正式 Excel 批量导入。
- `B-03 产品编码大小写敏感性`：阻塞唯一性规则和正式 Excel 批量导入。
- `B-08 Excel 导入的批次失败语义`：阻塞实际 Excel 导入；确认前只允许无持久化预检。
- `B-09 二维码图片文件名的非法字符契约`：阻塞不满足获批命名契约的产品创建与二维码下载。
- `B-04 误导入垃圾编码的处置`：阻塞产品状态模型定稿。
- `B-01 扫码后 PDF 的呈现方式`：阻塞公网扫码体验验收。
- `B-05 历史 PDF 保留策略`：阻塞存储容量和合规评审。
- `B-06 管理员密码重置责任`：阻塞上线运维准备与公网发布。
- `B-07 二维码地址泄露后的处置立场`：阻塞公网风险接受与人工发布批准。

## 下一个门禁

1. **G-01 需求确认（强制阻塞）**：优先由业务负责人关闭 B-02、B-03、B-08、B-09、B-04，并确认其余用户可见或责任归属事项。
2. **G-02 架构评审（强制阻塞）**：仅在需求确认通过后启动，评审技术栈、状态模型、安全边界、备份和回滚方案。

在这两个门禁通过前，不应开始功能实现、创建数据库或部署服务。
