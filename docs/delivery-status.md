# 交付状态

更新时间：2026-07-31

## 当前阶段

项目仍处于**治理与质量基线阶段**。`docs/requirements-v2.md` 是当前唯一有效的业务事实来源，并已补入同一 Excel 文件内规范化后重复编码的确认规则；`docs/requirements-v1.md` 仅保留为历史版本。G-01 中已经提出并收编的业务决定均已获得业务负责人确认。需求阶段参谋机制已经执行，`docs/advisor-review-g01.md` 如实保存业务负责人 → Orchestrator → Worker 转达的摘要；该记录不是 DeepSeek 原始输出，七项固定输出仍有缺失。G-01 仍需最终独立审核、业务负责人批准 PR 和门禁责任方明确结论，因此尚未通过，也未触发 G-02 架构评审。技术栈尚未选择，仓库没有功能代码、依赖清单、数据库或部署配置。

## 已完成事项

- 保持 `docs/requirements-v1.md` 字节级不变，并创建 `docs/requirements-v2.md` 作为当前唯一有效业务事实来源；v1 仅供历史追溯。
- 建立仓库协作治理规则，明确 Issue、独立 Worktree、独立分支、PR、人工合并和生产资产隔离要求。
- 登记用户可见业务待决项，并为技术不确定项采用安全、可测试、可回滚的默认值。
- 写入 2026-07-31 业务负责人确认的 7 条补充决策：Excel 混合失败语义、编码格式、编码大写保存、二维码文件命名、重复 PDF 拒绝、密码重置机制和历史 PDF 永久保留。
- 写入 2026-07-31 业务负责人确认的最后 4 条规则：扫码呈现允许设备差异、错误编码停用后另建、公开链接可转发且停用即失效、业务负责人本人执行密码重置。
- 写入 2026-07-31 业务负责人确认的同文件重复规则：同一份 Excel 内规范化后相同编码保留首次出现，后续重复行跳过；数据库既有重复与同文件后续重复共同计入“重复跳过数量”，均不属于格式错误，也不阻断无格式错误批次。
- 决策登记表的 B-01 至 B-09 均已确认，不再存在业务待确认或部分确认状态。
- 为被业务结论覆盖的 B-01、T-03、T-04、T-05 保留覆盖记录和迁移影响，未无痕改写决策历史。
- 建立从需求确认到回滚演练的质量门禁，区分强制阻塞与告警不阻塞，并定义可判定证据。
- 新增并修订 `docs/advisor-protocol-v1.md`，明确参谋职责、只读且无执行权与决定权、独立性、六个强制调用节点、客观跳过条件、七项固定输出、冲突处理，以及治理阶段可由 Worker 代为提交仓库文档的留存规则。
- 将六个参谋调用节点接入既有 G-01、G-02、G-10、G-17、G-18；相关门禁已明确七项输出完整且无未解决冲突才可能通过，参谋意见本身不构成门禁通过。
- **参谋机制执行状态：需求阶段已执行。** 本项目参谋已指定为 DeepSeek V4 Flash；其与本任务 Orchestrator、实施 Worker 均无角色重叠。`docs/advisor-review-g01.md` 保存转达摘要，并明确没有原始输出、执行时间或伪造证据链接；执行过参谋审视不等于 G-01 已通过。
- 建立通用忽略规则，覆盖本地机密、依赖、构建产物、缓存、上传、编辑器和日志文件。
- 新增 `agent-orchestrator.yaml` 项目约定文件，声明测试失败、审核要求修改、冲突和可合并状态均只提醒，不自动返工、不自动合并；其是否被工具自动读取尚待验证。

## 待处理事项

- **待业务负责人确认：三态优先级。** `CLAUDE.md` 隐含“停用优先于未上传”，但 `docs/requirements-v2.md` 保留的 v1 原文没有“启用但未上传”的前提；需确认产品同时停用且没有 PDF 时客户应看到哪句提示。确认前不得自行修改 v2 或实现该用户可见行为。
- 为 G-01 补齐转达摘要未提供的第 2、3、4、5、7 项、第 6 项具体理由、原始证据位置和冲突状态；“本次转达未提供”是诚实的缺失标记，不算七项完整输出。
- 完成最终独立审核，由业务负责人明确批准 PR #2，并由门禁责任方结合完整证据判定 G-01；参谋审视、业务决定或 PR 更新均不自动等于门禁通过。
- 只有 G-01 经人工判定通过后，才可由新任务另行启动 G-02 架构评审；本轮不自动触发。
- 技术栈确定后，为构建、类型检查、lint/format、测试、安全检查和容器构建补充精确命令与批准阈值。
- 为后续实现任务分别创建 GitHub Issue，并安排独立测试设计者。
- **进入技术方案前**：验证 AO 自动返工配置是否真正生效；`agent-orchestrator.yaml` 当前只表达待验证流程意图，不声称已被 AO 正式读取。本轮不执行该验证。
- **进入测试方案阶段**：设计二维码图片解码测试。本轮不进入测试方案阶段。
- **公网测试后**：校准 PDF、Excel、ZIP 的可配置安全默认上限。本轮不开展公网测试。
- **部署阶段**：完成密码重置操作手册。本轮不进入部署阶段。

## 阻塞事项

- **G-01 已登记业务决定：全部确认。** B-01 至 B-09 及 B-08 的同文件重复补充规则均已由业务负责人确认。
- **业务事实一致性阻塞项：1。** 停用状态与未上传状态的提示优先级尚待业务负责人确认；这是新发现的文档不一致，不推翻上述已确认决定。
- **已关闭：参谋人选未指定。** 业务负责人已指定 DeepSeek V4 Flash，且本次独立性核验无角色重叠。
- **流程阻塞项：G-01 尚未通过。** 参谋机制虽已执行，但现有记录只是七项不完整的转达摘要；同时缺最终独立审核、业务负责人 PR 批准和门禁责任方明确通过结论，因此不得进入架构评审或实现阶段。

## 下一个门禁

1. **G-01 需求确认（强制阻塞，尚未通过）**：先由业务负责人确认三态提示优先级，补齐参谋七项意见与冲突状态，完成最终独立审核和业务负责人对 PR #2 的明确批准，再由门禁责任方核对全部证据并作出判定。
2. **G-02 架构评审（未触发）**：只有 G-01 经人工判定通过后才能由后续任务另行启动；本轮不得自动进入该阶段。

在这两个门禁通过前，不应开始功能实现、创建数据库或部署服务。
