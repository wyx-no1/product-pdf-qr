# 交付状态

更新时间：2026-07-31

## 当前阶段

项目仍处于**治理与质量基线阶段**。`docs/requirements-v2.md` 是当前唯一有效的业务事实来源；`docs/requirements-v1.md` 仅保留为历史版本。B-01 至 B-12 均已由业务负责人确认并收编，业务决策与业务事实一致性阻塞项均已清零。需求阶段参谋已经完成两轮审视并达到默认上限，`docs/advisor-review-g01.md` 已收录 DeepSeek V4 Flash 的七项完整原始输出、六项风险和独立性核验限制；Orchestrator 接受全部发现，当前记录为无未解决冲突，但门禁责任方仍须核验身份与冲突状态。业务决策闭环和参谋意见补齐不等于 G-01 已通过；当前仍需最终独立审核和业务负责人明确批准 PR #2，之后由门禁责任方作出结论。G-02 未触发，也不得在本轮进入。技术栈尚未选择，仓库没有功能代码、依赖清单、数据库或部署配置。

## 已完成事项

- 保持 `docs/requirements-v1.md` 字节级不变，并创建 `docs/requirements-v2.md` 作为当前唯一有效业务事实来源；v1 仅供历史追溯。
- 建立仓库协作治理规则，明确 Issue、独立 Worktree、独立分支、PR、人工合并和生产资产隔离要求。
- 登记用户可见业务待决项，并为技术不确定项采用安全、可测试、可回滚的默认值。
- 写入 2026-07-31 业务负责人确认的 7 条补充决策：Excel 混合失败语义、编码格式、编码大写保存、二维码文件命名、重复 PDF 拒绝、密码重置机制和历史 PDF 永久保留。
- 写入 2026-07-31 业务负责人确认的最后 4 条规则：扫码呈现允许设备差异、错误编码停用后另建、公开链接可转发且停用即失效、业务负责人本人执行密码重置。
- 写入 2026-07-31 业务负责人确认的同文件重复规则：同一份 Excel 内规范化后相同编码保留首次出现，后续重复行跳过；数据库既有重复与同文件后续重复共同计入“重复跳过数量”，均不属于格式错误，也不阻断无格式错误批次。
- 写入 2026-07-31 业务负责人确认的三态优先级：扫码先判断停用，再判断有无当前 PDF；产品同时停用且无当前 PDF 时只显示“该产品资料已停用”。
- 写入 2026-07-31 业务负责人确认的 Excel 空白行规则：完全空白行忽略且不计数，非空格式错误仍导致整批失败。
- 写入 2026-07-31 业务负责人确认的停用作用范围：停用只限制公开访问，管理员在停用期间仍可上传、查看历史和恢复，重新启用后客户访问当时的当前有效 PDF。
- 决策登记表的 B-01 至 B-12 均已确认，不再存在业务待确认或部分确认状态。
- 为被业务结论覆盖的 B-01、T-03、T-04、T-05 保留覆盖记录和迁移影响，未无痕改写决策历史。
- 建立从需求确认到回滚演练的质量门禁，区分强制阻塞与告警不阻塞，并定义可判定证据。
- 新增并修订 `docs/advisor-protocol-v1.md`，明确参谋职责、只读且无执行权与决定权、独立性、六个强制调用节点、客观跳过条件、七项固定输出、冲突处理，以及治理阶段可由 Worker 代为提交仓库文档的留存规则。
- 将六个参谋调用节点接入既有 G-01、G-02、G-10、G-17、G-18；相关门禁已明确七项输出完整且无未解决冲突才可能通过，参谋意见本身不构成门禁通过。
- **参谋机制执行状态：需求阶段两轮已完成。** `docs/advisor-review-g01.md` 已从转达摘要更新为来源于 `automation/outputs/advisor-g01-full.txt` 的参谋原始输出，七项内容完整，推荐结论“调整”、六项风险及参谋无法直接核验 Orchestrator 身份和冲突关闭状态的限制均完整保留。
- 需求阶段参谋默认两轮上限规则已生效：第二轮后只有具备具体依据的不可逆损失、严重安全或法律合规问题可以继续阻塞 G-01；其他建议必须降级登记到对应后续阶段，不得反复重开需求阶段或静默丢弃。
- 建立通用忽略规则，覆盖本地机密、依赖、构建产物、缓存、上传、编辑器和日志文件。
- 新增 `agent-orchestrator.yaml` 项目约定文件，声明测试失败、审核要求修改、冲突和可合并状态均只提醒，不自动返工、不自动合并；其是否被工具自动读取尚待验证。

## 待处理事项

当前 G-01 下一步只剩两件事：

1. 完成最终独立审核，并由门禁责任方核验参谋、Orchestrator、Worker 身份与“无未解决冲突”记录。
2. 由业务负责人明确批准 PR #2；随后由门禁责任方结合全部证据判定 G-01，不得由本文件自动宣布通过。

## 已登记后续阶段待办（当前不启动）

- **G-02 技术方案阶段—风险 3**：明确“上传文件与某个历史版本内容相同但不同于当前版本”时的版本不变量边界。该建议不属于不可逆损失、严重安全或法律合规例外，按两轮上限规则降级；本轮不进入 G-02。
- **G-02 技术方案阶段—风险 5**：明确二维码 PNG 尺寸、容错率、清晰度等影响扫码成功率的规格。该建议不属于三类例外，按两轮上限规则降级；本轮不进入 G-02。
- **进入技术方案前**：验证 AO 自动返工配置是否真正生效；`agent-orchestrator.yaml` 当前只表达待验证流程意图，不声称已被 AO 正式读取。本轮不执行该验证。
- **进入测试方案阶段**：设计二维码图片解码测试。本轮不进入测试方案阶段。
- **公网测试后**：校准 PDF、Excel、ZIP 的可配置安全默认上限。本轮不开展公网测试。
- **部署阶段**：完成密码重置操作手册。本轮不进入部署阶段。
- 技术栈确定后，为构建、类型检查、lint/format、测试、安全检查和容器构建补充精确命令与批准阈值；为后续实现任务创建对应 Issue 并安排独立测试设计者。

## 阻塞事项

- **G-01 已登记业务决定：全部确认。** B-01 至 B-12 及 B-08 的同文件重复补充规则均已由业务负责人确认。
- **业务事实一致性阻塞项：0。** 停用状态与未上传状态的提示优先级已确认并收编进 v2，上一轮发现的文档不一致已关闭。
- **已关闭：参谋人选未指定。** 业务负责人已指定 DeepSeek V4 Flash，且本次独立性核验无角色重叠。
- **已关闭：参谋七项意见缺失。** 七项完整原始输出已收录，六项风险均有处置或后续阶段登记；Orchestrator 接受全部发现，记录为无未解决冲突，最终有效性仍由门禁责任方核验。
- **流程阻塞项：G-01 尚未通过。** 仍缺最终独立审核、业务负责人明确批准 PR #2 和随后由门禁责任方作出的通过结论，因此不得进入架构评审或实现阶段。

## 下一个门禁

1. **G-01 需求确认（强制阻塞，尚未通过）**：业务决定、业务事实一致性和参谋七项意见已经闭环；下一步仅为最终独立审核和业务负责人明确批准 PR #2，之后由门禁责任方核对全部证据并作出判定。

**G-02 架构评审仍未触发，不是当前下一步。** 在 G-01 被门禁责任方明确判定通过前，不得进入 G-02，不应开始功能实现、创建数据库或部署服务。
