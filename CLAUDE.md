# CLAUDE.md

本文档定义本仓库中人类与 Agent 共同工作的治理规则。它约束后续规划、实现、验证和发布活动，但不替代业务需求。

## 仓库与产品上下文

当前仓库是绿地项目：只有文档，没有功能代码、依赖清单、构建配置、数据库或部署配置，技术栈尚未选择。因此，目前不存在可执行的 build、lint、test 或 deploy 命令；技术栈经评审确定后，必须把可复现命令补入相应文档。

`docs/requirements-v1.md` 是当前版本唯一的业务事实来源。产品领域的核心不变量如下：

- 产品编码创建后不可修改；原因是它是稳定的业务身份，修改会破坏导入判重、文件命名和业务追溯。
- 产品不可物理删除，只能停用或重新启用；原因是永久二维码和历史资料必须保留可追溯关系。
- 一个产品在任意时刻只能有一个当前有效 PDF；上传新文件或恢复历史版本只改变当前版本指针，不能覆盖或删除历史事实。
- 二维码地址永久不变，实际打开的内容随当前版本指针变化；因此二维码不能绑定某个具体 PDF 文件地址。
- 扫码 URL 必须使用密码学安全、随机、不可枚举的公开标识，不得包含或可推导出产品编码、自增 ID 等内部标识。
- 公开扫码入口只有三种业务状态：产品停用时显示“该产品资料已停用”；产品启用但未上传 PDF 时显示“资料暂未上传”；产品启用且存在当前 PDF 时打开该 PDF。

第一期范围明确不包括 ERP 自动同步、微信小程序、多管理员与复杂权限、文件审批发布流程、防伪或单件追溯，以及单产品同时关联多个当前文件。任何人或 Agent 均不得把这些内容顺带加入实现。

## 业务事实与变更顺序

### 需求优先

**规则**：`docs/requirements-v1.md` 是业务事实来源；代码、测试、其他文档与它冲突时，以该文件为准，并立即停止冲突实现的继续交付。

**原因**：同一业务规则必须有唯一权威来源，否则并行工作会产生各自正确但彼此矛盾的实现。发现冲突不代表可以静默改需求，应先提交决策并由业务负责人确认。

### 先改业务文档，再改代码

**规则**：业务规则发生变化时，必须先更新需求文档或创建新版本需求文档，完成评审后才能修改代码和测试。不得只在 Issue、PR 评论或代码中改变业务行为。

**原因**：先记录业务决策可以保持需求、验收条件和实现同步，也让变更范围、迁移风险和回滚边界可审计。

## 角色与协作

### Orchestrator 只负责编排

**规则**：Orchestrator 原则上不直接实现功能，只负责拆解、规划、调度、监督、验收和跨任务协调；实际修改由被明确指派的 Worker 完成。只有为解除真实阻塞且重新分派明显不经济时，才可由人工明确授权例外。

**原因**：规划与实现分离可以减少同一主体既制定标准又自证通过的利益冲突，并保持并行任务的边界清晰。

### 每项开发工作必须有 Issue

**规则**：所有开发任务，包括功能、缺陷修复、重构、迁移和治理变更，都必须对应一个 GitHub Issue。Issue 至少写明背景、范围、非目标、可验证验收条件和责任人；PR 必须引用对应 Issue。

**原因**：Issue 提供稳定的工作入口和审计链，防止口头任务、隐含范围和无法追踪的变更进入主分支。

### Worker 隔离工作并通过 PR 交付

**规则**：Worker 必须使用独立 Worktree 和独立分支工作，通过 PR 提交变更；禁止直接向 `main` 提交或推送。一个分支只承载边界明确的一项任务。

**原因**：隔离工作区能减少并行修改互相污染，PR 则提供差异审查、自动验证和人工批准的统一入口。

### 禁止自动合并

**规则**：任何 Agent、脚本或流水线都不得自动合并 PR；合并必须由有权限的人类在查看变更与质量证据后明确批准。

**原因**：自动检查只能覆盖已编码的规则，无法替代对业务取舍、风险和发布时机的责任判断。

## 开发与验证纪律

### 开发前先定义验收条件

**规则**：实现开始前必须存在可观测、可测试、可判定的验收条件，包括成功路径、失败路径和关键边界。诸如“已完成”“工作正常”“体验良好”之类主观陈述不能作为验收条件。

**原因**：预先定义可验证结果可以限制范围，并避免实现者在完成后重新解释“成功”的含义。

### 不得通过弱化测试换取绿色流水线

**规则**：严禁删除、跳过、标记 `skip`/`xfail`、注释掉、放宽断言、缩小覆盖范围或降低阈值来让流水线变绿。若测试本身错误，必须保留证据、说明原因，并在同一 PR 中以不降低业务保护强度的方式修正。

**原因**：绿色流水线只有在检查仍然有效时才是交付证据；弱化测试会隐藏缺陷并制造虚假的安全感。

### 完成声明必须有可复现证据

**规则**：Agent 自称“已完成”不构成交付证据。每项交付必须附带可由他人复现的验证产物，例如完整命令与退出码、测试结果、静态检查报告、截图、差异清单或人工验收记录。

**原因**：可复现证据把主观陈述转化为可审计事实，使审查者无需信任执行者本身。

### 强制门禁失败即停止推进

**规则**：`docs/quality-gates-v1.md` 中标记为“强制阻塞”的门禁只要失败、证据缺失或结果不可判定，就不得进入下一阶段，不得发布，也不得用“后续补测”绕过。

**原因**：门禁的价值在于风险暴露时真正阻止扩散；允许带失败前进等同于没有门禁。

## 数据与环境安全边界

### 禁止接触正式资产

**规则**：Agent 严禁访问、下载、上传、修改或删除正式服务器、正式数据库和真实业务文件。开发与验证只能使用本地或专用测试环境中的合成数据；任何生产操作都必须由获授权的人类按发布流程执行。

**原因**：Agent 任务不应拥有生产数据权限。把环境与数据边界前置，可以避免误操作、数据泄露和不可逆业务影响。

## 文档维护

- 文档与业务说明使用中文；代码标识符、分支名和 commit message 使用英文。
- 技术栈确定后，补充构建、类型检查、格式化、lint、测试、容器构建和单项测试的精确命令，但不得以补命令为由降低门禁标准。
- 决策状态应记录在 `docs/decision-register-v1.md`。业务负责人确认后，记录决策人、日期和影响；不得无痕覆盖历史取舍。
